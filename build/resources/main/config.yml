blueprint:
  default-material: PAPER
  default-name: 建筑蓝图
selection:
  max-x: 300
  max-y: 300
  max-z: 300
  banned-materials:
    - BARRIER
    - BEDROCK
    - END_PORTAL
    - END_PORTAL_FRAME
    - CHAIN_COMMAND_BLOCK
    - MOB_SPAWNER
preview-world:
messages:
  first-point-selection: First point selected.
  second-point-selection: Second point selected.
  section-selected: Section selected.
  selection-cleared: Selection cleared.
  material-list: "%MATERIAL x %AMOUNT"